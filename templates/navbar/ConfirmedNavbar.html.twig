<nav class="navbar navbar-expand-lg navbar-light bg-light">

	<a class="navbar-brand" href="/confirmed/home"><img src="/images/matcha-love-logo.png" alt="matcha-logo" style="width:40px;"></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarNav">

		<ul class="navbar-nav">

			<li class="nav-item">
				<a class="nav-link nav-link-pink-first" href="#why-matcha-section">Match</a>
			</li>

			<li class="nav-item">
				<a class="nav-link nav-link-pink-first" href="#why-matcha-section">Inbox</a>
			</li>

			{#admin part#}
			{% if is_granted('IS_AUTHENTICATED_REMEMBERED') and is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link nav-link-pink" href="/admin">Admin</a>
				</li>
			{% endif %}


		</ul>

		<ul class="navbar-nav ml-auto">
			{% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
				<li class="nav-item">
					<a class="btn nav-btn-sign-in" onclick="showModalLogin()">Sign in</a>
				</li>
			{% else %}
				<li class="nav-item">
					<div class="dropdown">

						{#img of user trigger the dropdown menu #}
  						<img src="{{ asset('images/user/default-user.png') }}" class="dropdown-toggle img-dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">

						<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
							<div class="card card-user" style="width: 18rem; border: none;">
								<div class="card-body text-center">
									<div class="header-card-div">
	  									<img src="{{ asset('images/user/default-user.png') }}" class="img-card-body" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								    	<h5 class="card-title">{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %} {{name}}{% endif %}</h5>
									</div>
								</div>
								{% if confirmed is same as(true) %}
								<div class="card-body text-center">
									<a href="#" > your match </a>
								</div>
								{% endif %}
								<div class="card-footer">
									<div class="row">
										<a class="nav-btn-loggout dropdown-item col-sm" href="{{ path('app_logout') }}">sign out</a>
										<a class="nav-btn-loggout dropdown-item col-sm" href="{{ path('app_edituser') }}">edit your profile</a>
									</div>
								</div>
							</div>
						</div>
					</div>
				</li>
			{% endif %}
		</ul>

	</div>

</nav>