{% extends 'home/layout.html.twig' %}

{% block body %}
  {{ parent() }}
  <header class="masthead">
    <div class="container">
      <div class="intro-text">
        <div class="intro-lead-in">
          Get True Story{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}{{
            user_name
          }}{% endif %}
        </div>
        <div class="intro-heading text-uppercase">
          <span class="Text-Pink">Ma</span><span class="Text-White">tcha</span>
        </div>
        {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
          <!-- Bouton servant à activer la modale -->
<a data-target="{{ path('app_register') }}" data-tooltip="voir"
   class="modal-trigger btn btnSubmit right-align">Register</a>

            <!-- Structure de la modale -->
<div id="modal1" class="modal fade">
  <div class="modal-dialog" role="document">
    <div class="modal-content">

    </div>
  </div>
</div>
        {% endif %}
      </div>
    </div>
  </header>
{% endblock %}